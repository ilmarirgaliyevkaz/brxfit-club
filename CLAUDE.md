# CLAUDE.md — brxfit.club

Контекст проекта для Claude Code. Читать в начале каждой сессии.

## Что это

Рекламные лендинги фитнес-клуба **Bronx Fitness** (Астана, Казахстан).
Цель сайта одна — заявка в WhatsApp. Всё остальное вторично.

Общаемся по-русски.

## Страницы

| Страница | Назначение | Реклама (UTM campaign) |
|---|---|---|
| `/` | клуб целиком, 16 направлений | — |
| `/hyrox` | Hyrox | `hyrox-pmax` |
| `/fight` | взрослый бокс | `fight-pmax` |
| `/fire` | кардио-бокс (**унисекс**, не женский) | `fire-pmax` |
| `/teens` | бокс 13–17 лет | `teens-pmax` |

## Жёсткие правила (не нарушать)

- **Оффер только один: 3 пробных занятия за 2990 ₸.** Не «первая тренировка бесплатно», не абонемент 29 990 ₸.
- **Никаких выдуманных данных.** Нет фактов — ставить `TODO(content):`, а не сочинять метрики, отзывы и кейсы.
- **Локация только ЭКСПО** — просп. Мангилик Ел 55/3, павильон B2.4. Не перечислять залы bronx.kz.
- **Метка источника — в начале WhatsApp-сообщения**, чтобы была видна в превью чата:
  `Заявка с сайта brxfit.club (Hyrox). Здравствуйте! ...`
- **Тренеры** — «мастера спорта и призёры турниров». Без «15 тренеров в 3 залах».
- **`BRONX J LO` удалён** — отсылка к реальному человеку, риск по правам. Не возвращать без прямого указания.

## Реальные данные клуба

- 2ГИС: рейтинг **4.7**, **314 отзывов**; часы **07:00–23:00**
- Instagram: `@bronxfitness_expo`
- WhatsApp CTA: **77071309980**
- geo: 51.088808 / 71.415362

## Стек и бренд

- **Astro 4 + Tailwind 3 + TypeScript** (strict). Статика, без API.
- Токены: фон `#0A0A0A`, золото `#FFD700`, шрифт Inter.
- Классы-токены: `bg`, `surface`, `fg`, `muted`, `gold`, `line`. Хардкод цветов не использовать.

## Структура

```
src/
├── data/
│   ├── programs.ts    # каталог направлений; flagship + href + порядок витрины
│   └── site.ts        # GA4_ID, номер WhatsApp, общие константы
├── layouts/AdLandingLayout.astro   # meta, JSON-LD, gtag, трекинг кликов
├── components/sections/            # секции страниц
├── components/ui/                  # WhatsAppButton, ProgramCard
└── pages/                          # 5 страниц
```

- Витрина «С чего начинают» = `flagshipOrder` в `programs.ts`. Порядок задан вручную (Hyrox первый), **не по посещаемости**.
- Ссылка направления на свою страницу = поле `href` в `programs.ts`. `ProgramCard` сам отрисует `<a>` и стрелку.

## Аналитика

- GA4: **G-VVJ3Q40M39**, ключевое событие **`generate_lead`** (клик по WhatsApp).
- Google Ads: аккаунт **811-040-1866** (общий с bronx.kz — не путать конверсии).
- gtag живёт в `AdLandingLayout.astro`.
  ⚠️ Вставлять исполняемый JS через `set:html`. Через `define:vars` скрипт попадает в страницу мёртвым текстом — этот баг уже ловили, GA4 молчал.

## Ассеты

- Видео есть **только для Hyrox**: `public/video/hyrox/{hero,overview}.mp4`.
  Роликов fire/fight/women в репозитории нет — не ссылаться на них (плитки станут чёрными).
- Постеры витрины — OG-картинки: `public/og-{hyrox,fight,fire,teens}.png`.
- `public/og-image.png` для главной **отсутствует** → превью главной при шеринге битое. TODO.

## Деплой

- GitHub `ilmarirgaliyevkaz/brxfit-club` → **Vercel** (автосборка на push в `main`).
- DNS в **Cloudflare, только DNS-only (серое облако)** — проксирование ломает выпуск SSL на Vercel.
- Апекс `brxfit.club` — основной, `www` редиректит.

Перед коммитом:
```
npm run build
```

## Особенности окружения

- macOS, Node v26.
- **Умные/кудрявые кавычки ломают терминал.** В командах — только прямые кавычки или без кавычек.

## Смежное

- `bronx.kz` — отдельный сайт на hoster.kz. По закону РК домены `.kz` обязаны резолвиться на серверы физически в Казахстане, поэтому Vercel/Tilda для него не подходят. Этот проект (`brxfit.club`) — не `.kz`, ограничение не действует.

## Как со мной работать

- Нужны структурированные варианты выбора в точках решения, а не открытые вопросы.
- Честный pushback обязателен: если логика хромает — сказать прямо, а не соглашаться.
- Уже принятые решения не переобсуждать.
- Показывать реальный результат, а не приблизительный пересказ.
